# Задание 0
## На вход алгоритма попадётся натуральное число №. Алгоритм строит пл нему новое число 8 следуещим образом.
```
 
    n = int(input()) #вводится переменная в строку
    r = bin(n)[2:] № переводит переменную в двоичную систему без лишних символов
    print(r) #выводит чисто на экран
    if r.count('1') % 2 == 0: #определяется количество чисел,  если количество чисел честно то:
      r += '0' #k переменной r прибавляется 0
    else: #иначе если число нечетное:
    r =+ '1' #k переменной r прибавляется 1
    print(r) #выводит чисто на экран
    if r.count('1') % 2 == 0:  #определяется количество чисел,  если количество чисел честно то:
    r += '0' #k переменной r прибавляется 0
    else: #иначе если число нечетное:
    r += '1' #k переменной r прибавляется 1
    print(r) #выводит чисто на экран



 
